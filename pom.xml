<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>ca.weblite</groupId>
    <artifactId>swingwebbrowser</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>Swing Web Browser</name>
    <description>A demo web browser built on top of the swingwebview component.</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>21</maven.compiler.release>
        <exec.mainClass>ca.weblite.swingwebbrowser.SwingWebBrowser</exec.mainClass>
    </properties>

    <dependencies>
        <!-- swingwebview. 1.6.2 fixes the lightweight (GTK offscreen) engine:
             gtk_off_create_engine connected the "external" script-message
             channel twice on the same WebKitUserContentManager, and
             g_signal_connect is additive, so both callbacks ran and every
             message the page posted arrived in Java twice — doubling every
             addJavascriptFunction call, every addJavascriptCallback, console
             capture and evalAsync resolution. Linux only: resolveDefaultMode
             picks LIGHTWEIGHT there and only there, and the heavyweight engine
             always connected once. Drop-in over 1.6.1; no API change.

             1.6.1 fixed the macOS credential store: a credential saved with an
             empty user name (every two-step login — Google, Microsoft, Okta)
             was written to the Keychain and then invisible to every read, and
             the shim now carries the user name across the two steps.

             This project declares no ca.weblite:webview-run, so it has ONE copy
             of ca.weblite.webview.* and one set of natives on its classpath.
             The lockstep hazard documented in litecode and agentic-app-framework
             — where a shaded copy inside webview-run competes with this
             dependency and Class-Path order silently decides which native is
             loaded — does not apply here, and this pin can move on its own. -->
        <dependency>
            <groupId>ca.weblite</groupId>
            <artifactId>webview</artifactId>
            <version>1.6.2</version>
        </dependency>
        <dependency>
            <groupId>com.formdev</groupId>
            <artifactId>flatlaf</artifactId>
            <version>3.5.4</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
            </plugin>

            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.5.0</version>
                <configuration>
                    <mainClass>${exec.mainClass}</mainClass>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.1</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>${exec.mainClass}</mainClass>
                            <addClasspath>true</addClasspath>
                            <classpathPrefix>lib/</classpathPrefix>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.6.1</version>
                <executions>
                    <execution>
                        <id>copy-dependencies</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy-dependencies</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/lib</outputDirectory>
                            <overWriteReleases>false</overWriteReleases>
                            <overWriteSnapshots>false</overWriteSnapshots>
                            <overWriteIfNewer>true</overWriteIfNewer>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
